Frontend (React)

Install dependencies and start dev server:

```bash
cd frontend
npm install
npm start
```

The frontend expects backend at `http://localhost:8080` and a WebSocket at `ws://localhost:8080/ws/slots`.
